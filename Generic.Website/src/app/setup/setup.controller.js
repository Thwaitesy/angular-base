(function () {
    'use strict';

    angular
        .module('setup.module')
        .controller(setupController);

    setupController.$inject = [''];
    function setupController() {
        var vm = this;

    };

})();